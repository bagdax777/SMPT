<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SMPT" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/purple_500</item>
        <item name="colorControlNormal">@color/white</item> <!-- Kolor linii pod spodem -->
        <item name="colorControlActivated">@color/white</item> <!-- Kolor linii po aktywacji -->
    </style>

    <style name="Theme.SMPT" parent="Base.Theme.SMPT" />
    <style name="CustomTextInputEditText" parent="Widget.MaterialComponents.TextInputEditText.FilledBox">
        <item name="android:textColorHint">@color/white</item>
        <!-- Kolor linii pod tekstem oraz hinta po aktywacji -->
        <item name="colorControlActivated">@color/white</item>
        <!-- Kolor ramki TextInputLayout -->
        <item name="boxStrokeColor">@color/white</item>
    </style>
</resources>